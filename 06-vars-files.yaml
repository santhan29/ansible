- name: variables frm files
  hosts: web 
  vars_files:  
  - vars.yaml
  tasks: 
  - name: print the info 
  ansible.builtin.debug:
    msg: "hi i am learning {{course}}, duration is {{duration}}, trainer is {{trainer}}"